{
  "name": "json-server",
  "version": "1.0.0",
  "description": "json-server",
  "scripts": {
    "test": "json-server --watch db.json --port=3000",
    "demo": "json-server --watch demo.js --port=3000",
    "mockdb": "json-server --watch mockdb.js --port=3000"
  },
  "repository": {
    "type": "git",
    "url": "https://gitee.com/valten/json-server.git"
  },
  "keywords": [
    "json-server"
  ],
  "author": "valten",
  "license": "ISC"
}
